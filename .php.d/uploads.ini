; The dev server must accept what DocumentStoreRequest promises: 100M per
; document, one file per request. Loaded via PHP_INI_SCAN_DIR in the
; composer "dev" script; production PHP/nginx must be raised separately.
upload_max_filesize = 100M
post_max_size = 105M
